# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install -g @angular/cli
    npm install
    npm run build
  displayName: 'npm install and build'

- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: 'dist'
    includeRootFolder: false
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
    replaceExistingArchive: true
  displayName: 'Archiving Files'

- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'http://ftp.simbayu.in'
    username: 'oslabndb'
    password: 'Adage1720@#'
    rootDirectory: '$(Build.ArtifactStagingDirectory)'
    filePatterns: '$(Build.BuildId).zip'
    remoteDirectory: '/dev.simbayu.in'
    clean: true
    cleanContents: true
    preservePaths: true
    trustSSL: true
  displayName: 'Uploading'

- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'http://ftp.simbayu.in'
    username: 'oslabndb'
    password: 'Adage1720@#'
    rootDirectory: '$(Build.ArtifactStagingDirectory)'
    filePatterns: ''
    remoteDirectory: ''
    clean: true
    cleanContents: true
    preservePaths: true
    trustSSL: true
    customCmds: 'unzip /dev.simbayu.in/$(Build.BuildId).zip'
  displayName: 'Unzip'

- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'http://ftp.simbayu.in'
    username: 'oslabndb'
    password: 'Adage1720@#'
    rootDirectory: '$(Build.ArtifactStagingDirectory)'
    filePatterns: ''
    remoteDirectory: ''
    clean: true
    cleanContents: true
    preservePaths: true
    trustSSL: true
    customCmds: 'delete /dev.simbayu.in/$(Build.BuildId).zip'
  displayName: 'Deleting zip'